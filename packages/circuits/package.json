{
  "name": "@privacy-paymaster/circuits",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bash scripts/build.sh",
    "test": "mocha test/**/*.js --timeout 60000",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.4",
    "mocha": "^10.4.0",
    "chai": "^4.4.0"
  }
}
